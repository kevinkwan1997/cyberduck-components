import type { Button } from "$shared/base-components/button/button";

export class HeaderMenuButton implements Button {
    public isMenuOpen: boolean = false;
    public onClick(): void {
       
    }
}